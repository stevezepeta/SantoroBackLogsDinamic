spring.application.name=backlogs
spring.profiles.active=mail

# ===== CONFIGURACION DE CORREO (Configurable por ambiente) =====
spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${MAIL_USERNAME:soporte.tecnico@grupo-santoro.com.mx}
spring.mail.password=${MAIL_PASSWORD:wnnkjroexgypcpss}

spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.ssl.enable=false
spring.mail.properties.mail.smtp.ssl.trust=${MAIL_HOST:smtp.gmail.com}

app.mail.from=${MAIL_FROM:soporte.tecnico@grupo-santoro.com.mx}
app.mail.from-name=${MAIL_FROM_NAME:Backlogs Santoro - Sistema Principal}

# ===== CONFIGURACION DE WEBSOCKET - AMBIENTE PRINCIPAL (Puerto 8040) =====
# Origenes permitidos para WebSocket del sistema principal
# Produccion: ws://dashboard-api.grupo-santoro.com.mx/ws
# Desarrollo: ws://localhost:8040/ws
app.websocket.allowed-origins=${WEBSOCKET_ALLOWED_ORIGINS:http://localhost:3000,https://dashboard-api.grupo-santoro.com.mx}

eva.openai.api-key=${EVA_OPENAI_API_KEY:}
#spring.profiles.active=dev

multitenant.require-api-key=true
multitenant.legacy-tenant-filter-enabled=false
multitenant.header.tenant=X-Tenant

security.jwt.secret=KoSd8fKjYA1Bdy8nFHAYZlcAhj9bCIGQGGrhKPAMmGo=
security.jwt.issuer=backlogs
security.jwt.ttl-minutes=120

server.error.include-message=always
server.error.include-binding-errors=always
spring.mvc.log-request-details=true

springdoc.api-docs.path=/v3/api-docs
springdoc.swagger-ui.path=/swagger-ui.html
# Esto ayuda si tienes rutas muy complejas
springdoc.show-actuator=true
springdoc.model-and-view-resolved-as-json=true


# Alertas para cada system - TEMPORAL CAMBIAR PARA QUE ESTE EN EL FRONT ESTA CONFIGURACION -
alert.thresholds.default.warn=0.05
alert.thresholds.default.crit=0.15

alert.thresholds.WINDOWS_SERVER_PRINCIPAL.warn=0.10
alert.thresholds.WINDOWS_SERVER_PRINCIPAL.crit=0.20

alert.thresholds.WORKSTATIONS_SANTORO.warn=0.20
alert.thresholds.WORKSTATIONS_SANTORO.crit=0.50

alert.thresholds.TICKETS.warn=0.05
alert.thresholds.TICKETS.crit=0.10

